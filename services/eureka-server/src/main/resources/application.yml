spring:
  application:
    name: eureka-server
  profiles:
    active: development
  config:
    import: configserver:http://localhost:8888
  cloud:
    config:
      username: admin
      password: admin
#server:
#  port: 8761
#eureka:
#  client:
#    register-with-eureka: false
#    fetch-registry: false
