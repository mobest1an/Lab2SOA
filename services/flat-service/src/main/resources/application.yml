server:
  ssl:
    key-store: classpath:keystore/iver.p12
    key-store-password: iveriver
    key-store-type: PKCS12
    key-alias: flat
    key-password: iveriver
    enabled: true


spring:
  jpa:
    generate-ddl: true
  datasource:
    url: jdbc:postgresql://localhost:54320/flat
    username: flat
    password: flat
    driver-class-name: org.postgresql.Driver
logging:
  level:
    web: debug
