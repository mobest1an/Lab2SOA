server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
#          uri: ${CONFIG_GIT_URI:file://C:/Users/Ivan/IdeaProjects/LabSoa/lab-soa-git-config}
          uri: ${CONFIG_GIT_URI:file://.}
          clone-on-start: true
  security:
    user:
      name: admin
      password: admin